# bh-wp-mailboxes — Execution Plan

A phased plan for an AI coding agent (Claude Sonnet 4.6) to bring this library to a releasable state: green test suites, demonstration via the development-plugin, working CI/CD, and accurate README documentation.

## Context (read before starting)

This is a **Composer library** (`brianhenryie/bh-wp-mailboxes`), not a standalone plugin. It downloads emails into WordPress via IMAP (`Providers/Imap/class-imapengine-imap-email-fetcher.php`, using `directorytree/imapengine`) or the Gmail API (`Providers/Gmail_API/class-gmail-email-fetcher.php`), saves them to a custom post type via `includes/api/repositories/class-email-wp-post-repository.php`, and fires `bh_wp_mailboxes_fetch_emails_saved_{$plugin_slug}` for the parent plugin to act on. Entry point: `API::check_email()` in `includes/api/class-api.php`, scheduled by `includes/WP_Includes/class-cron.php`.

Read these before any work: `README.md`, `ARCHITECTURE.md`, `CLAUDE.md`, `includes/api/class-api.php`, `development-plugin/development-plugin.php`.

## Ground rules (apply to every task)

1. **Lint everything you touch:** `vendor/bin/phpcbf <files>`, then `vendor/bin/phpcs <files>`, then `vendor/bin/phpstan analyse --memory-limit 1G`. All must pass before a task is "done". (`composer lint` runs all three.)
2. **Mockery in unit tests** (not raw PHPUnit mocks). Unit suite uses WP_Mock + Patchwork; see `tests/unit/class-unit-testcase.php`.
3. **Playwright tests for any UI change**, in `tests/e2e-pw/specs/`. Run with `npm run wp-env:start` then `npm run test:e2e`.
4. **Test naming convention:** mirror the source path, e.g. `includes/api/class-api.php` → `tests/unit/api/class-api-unit-Test.php`. One test class per source class. Use full, unambiguous names in test method names, e.g. `test_check_email_skips_account_after_recent_failed_login()`.
5. **Suites:** `vendor/bin/codecept run unit` (no WordPress), `run wpunit` (WPLoader/database), `run integration` (full WordPress), `run contract` (live mailbox credentials — never run in CI; skip when `test-credentials/` env vars are absent).
6. **Do not invent features.** The README's CLI section and credentials-storage musings are open design questions — move them to `docs/open-questions.md`, do not implement them.
7. **Never commit credentials** or anything from `test-credentials/`. Never weaken `development-plugin/class-authentication.php` guards (it must only load inside wp-env).
8. **One phase per branch/PR.** Within a phase, commit per task with descriptive messages.
9. **Stop and ask the user** at any checkpoint marked ⚠️.

---

## Phase 0 — Baseline: make everything green ✅ (PR #1, merged)

Goal: trustworthy local signal. No new features.

- [x] 0.1 Run `composer install`, then `vendor/bin/codecept run unit` and `vendor/bin/codecept run wpunit`. Fix failures. Commit `8a71f30` ("Comment out erroring code") left dead/commented code — find it (`git show 8a71f30`), then fix or delete it.
- [x] 0.2 Run `composer lint` repo-wide on `includes/` and `development-plugin/`. Fix all phpcs and phpstan errors.
- [x] 0.3 Ported to `tests/contract/Providers/Imap/class-imapengine-email-fetcher-contract-Test.php`; dead `Ddeboer_Imap` file deleted. The `integration` suite survives (hooks + API tests) and has its own workflow.
- [x] 0.4 `tests/_data/temp/` is gitignored and untracked. ⚠️ Still open: the ~35 real `.eml` files remain in git history — a `git filter-repo` rewrite is warranted; flag before any history-sensitive work.
- [x] 0.5 wp-env boots, development-plugin activates, both e2e specs pass.

**Phase 0 also delivered most of the originally planned Phase 1:** the old PHP 7.4-era workflows were replaced rather than rewritten per the original task list. Current workflows (all PHP 8.4): `phpcbf.yml` (phpcbf auto-commit on master + phpcs annotations via cs2pr; on PRs, fails only on errors in changed lines via `kamazee/pr-filter`), `phpstan.yml` (annotations repo-wide; fails on `includes/` on master, on changed files in PRs), `unit-coverage.yml` (unit + wpunit with mysql:8.0, coverage merged and published to gh-pages, `.github/coverage.svg` badge, PR coverage comment), `integration.yml` (codecept integration suite), `e2e.yml` (Playwright against wp-env, report artifacts). `acceptance.yml`, `deploy.yml`, and `release.yml` are deleted; `dependabot.yml` (daily, composer + npm + github-actions) is in place; the `gh-pages` branch exists.

## Phase 1 — CI/CD hardening

Goal: close the gaps the Phase 0 workflow rewrite left open. Do **not** restructure the existing five workflows — the auto-commit phpcbf approach and the PR diff-filtering are deliberate; keep them.

- [x] 1.1 Aligned action versions across all five workflows: `actions/checkout@v6`, `actions/upload-artifact@v7`, `shivammathur/setup-php@2.37.2`, `stefanzweifel/git-auto-commit-action@v7.1.0`. Also added artifact upload to `phpcbf.yml`.
- [x] 1.2 Fixed `e2e.yml` package-manager mix-up: `yarn playwright install` → `npx playwright install --with-deps chromium`.
- [x] 1.3 Coverage gate: set `percentage: 33`, `exit: true`, `metric: statements` (33.3% measured; `metric: elements` was 31.9% — pinned to `statements` for consistency with local measurement).
- [x] 1.4 New `release.yml`: on `v*.*.*` tag, verifies `CHANGELOG.md` contains the tag version. ⚠️ Confirm `brianhenryie/bh-wp-mailboxes` is registered on Packagist before tagging.
- [x] 1.5 Throwaway PR #8 validated diff-filtering: phpcs and phpstan failed on touched-file errors; untouched-file errors were annotations-only (phpstan skips non-`includes/` paths; phpcs uses `kamazee/pr-filter`). Discovery: coverage action defaults to `metric: elements` — fixed in 1.3 update.
- [ ] 1.6 ⚠️ Branch protection on `master`: ask the user to require the five workflow checks before merge (or set via `gh api` if permitted) — without it, the PR-only failure logic in `phpcbf.yml`/`phpstan.yml` is advisory.

**Done when:** a test PR shows annotations + coverage comment and all five workflows are required and green; the coverage gate demonstrably fails below threshold; a test tag passes `release.yml`.

## Phase 2 — Unit test coverage of the core pipeline

Goal: every class in `includes/` has a test file. Current coverage gaps — these classes have **zero tests**:

| Class | Suite | Focus |
|---|---|---|
| `ImapEngine_Imap_Email_Fetcher` | unit | message→`BH_Email` mapping with Mockery-mocked ImapEngine client; date-range filtering |
| `Gmail_Email_Fetcher` | unit | same, mocking `Google\Service\Gmail`; the existing contract test needs live credentials |
| `Email_Account_WP_Post_Repository` | wpunit | save/retrieve account; address immutability rule from README |
| `BH_Email_Account` (model) | unit | construction, getters |
| `BH_Email_Factory` | unit | building `BH_Email` from parsed `IMessage` (use `tests/_data/wpunit/*.eml` fixtures) |
| `BH_Email_Query` + `WP_Post_Query_Abstract` + `WP_Posts_Query_Order` | wpunit | meta-query building; the `is_read_remote` null-handling TODO |
| `Admin_Notices` | unit | notice added on auth failure, cleared on success |
| `Status_View` | unit | currently a TODO stub — see 2.3 |
| `BH_WP_Mailboxes_Hooks` | unit | every `add_action`/`add_filter` registered (integration test exists, no unit test) |
| `BH_Email_Account_CPT` | wpunit | CPT registration args |
| settings traits (`trait-bh-wp-mailboxes-settings-defaults.php`, `trait-email-account-settings-defaults.php`) | unit | default values |

And these are **thin** (≤1 test method): `tests/wpunit/API/class-api-wpunit-Test.php`, `tests/wpunit/repository/class-email-wp-post-repository-WPUnit-Test.php`, `tests/wpunit/Model/class-bh-email-wpunit-Test.php`, `tests/wpunit/WP_Includes/class-cron-wpunit-Test.php`.

Tasks (each = tests first, then resolve the embedded TODO the tests pin down):

- [x] 2.1 `API::check_email()` branch coverage in `tests/unit/api/class-api-unit-Test.php` (moved from `tests/unit/Providers/` per naming convention): failed-login backoff skip, unknown-credentials-type warning, stub fetcher via filter, first-run one-week lookback + stored-time lookback, dedup reject before `save_all()`, both actions firing with the saved emails, `check_email_for_account()` (explicit since + default lookback). TODO resolved as explicit `Email_Account_WP_Post_Repository::clear_last_failed_login_time()` — **the parent plugin must call it from its settings-save handler**. Also: fetch exceptions now record `last_failed_login_time` (previously nothing set it, so the 4-hour backoff was dead code — note: *any* fetch exception triggers backoff, not just auth failures); fixed latent bug where `BH_Email_Account_Query`/`BH_Email_Query` dropped `post_id`, so both repositories' `update()` called `wp_update_post()` without `ID` (regression-tested in `tests/unit/api/repositories/queries/`). Remaining gaps: `update()` round-trips still need the planned wpunit tests (table row above); `clear_last_failed_login_time()` has unit coverage only; wpunit suite not runnable in the sandbox (no MySQL) — verify in CI.
- [x] 2.2.1 `Email_WP_Post_Repository`: dedup behavior (same guid fetched twice → one post);
- [x] 2.2.2 `is_read_remote` determination: captured at fetch time (IMAP `\Seen` / Gmail `UNREAD` label) and stored; `get_is_marked_read()` re-checks via a stored remote id (IMAP UID + UIDVALIDITY + folder, Gmail message id) with a Message-ID search fallback. See `Remote_Email_Coordinates` / `Fetched_Email`.
- [x] 2.2.3 attachment saving via `bh-wp-private-uploads`: whWe nen the private-uploads `API_Interface` is supplied (threaded from `API` through `save_all`/`save_new`), each attachment is saved via `move_file_to_private_uploads_and_create_post()` and the post ids stored on the `BH_Email` (`attachment_ids`). Factory now distinguishes disabled (`null`) from none (`[]`). Caveat: `move_file_to_private_uploads()` checks `current_user_can('upload_files')`, which fails in pure cron — per-attachment failures are logged and the email still saves.
- [x] 2.3 `Status_View` (`includes/admin/class-status-view.php` is a TODO stub): implement minimally — per account: last fetched time, last failure time, email count — with wpunit tests and a Playwright spec (rule 3).
- [x] 2.4 Fetcher mapping tests using the sanitized fixtures: `tests/unit/Providers/Imap/class-imapengine-imap-email-fetcher-unit-Test.php` (Mailbox mocked via reflection; real `MessageCollection` of mocked `MessageInterface`s; asserts UID/folder/UIDVALIDITY/Message-ID/`\Seen` mapping + the since-time date filter) and `tests/unit/Providers/Gmail_API/class-gmail-email-fetcher-unit-Test.php` (extracted a `Gmail_Email_Fetcher::get_gmail_service()` seam to inject a mock service; asserts Gmail-id→remote_uid mapping + `UNREAD`-label read state). `BH_Email_Factory::from_wp_post` mapping was already covered by the factory wpunit test.
- [x] 2.5 Raised the `unit-coverage.yml` ratchet 33% → **45%** (`metric: statements`). Measured statement coverage of `includes/` after this phase is **48.0%** (1551 statements, 744 covered; unit + wpunit merged via `phpcov`). The gate is set just under the measured figure as a regression floor; pushing toward 70% needs the still-untested admin view classes (`Single_Email_View`, `Emails_List_Page`, etc.) and provider edge paths covered.

**Done when:** every class in `includes/` has a corresponding test file and the table above is empty.

## Phase 3 — development-plugin as the living example ✅ (merged to master)

Goal: a developer (or an e2e test) can exercise the full pipeline with zero real mailboxes.

**Verdict (reviewed 2026-07-01): complete.** The `phase-3` branch is merged into `master` (tip `37a7d02`, clean tree). Every code-verifiable "Done when" criterion is met: `Mock_Mailbox_Fixtures_Connection` + its unit test; named `Mailbox_Settings` / `Fixtures_Account_Settings` + unit tests; dev REST (`bh-wp-mailboxes-dev/v1`) grew beyond plan to `/status`, `/accounts`, `POST|DELETE /emails`, `POST /fetch`; Reset button wired with nonce via `handle_reset_submission()`; the `bh_wp_mailboxes_credentials` arg order is `( null, $plugin_slug, $account )` at all four call sites with regression test `test_test_connection_resolves_credentials_via_filter_with_correct_argument_order`; `Example_Integration` + unit test. Verification caveats: this review could not execute `composer lint` / the unit suite (no PHP in the review sandbox) nor the browser fetch flow — re-run the verification checklist locally before calling the phase formally closed. Carried forward (not blockers): three anonymous `BH_WP_Mailboxes_Settings_Interface`/`Email_Account_Settings_Interface` impls remain (`development-plugin.php`, `mailboxes/class-imap.php`, `class-gmail-api.php`) — Phase 5 README linking; the resolution filter is still named `bh_wp_mailboxes_provider_for_account` despite the connection rename (small rename + deprecation shim, any phase); the e2e-infrastructure items deferred from 3.3 are now Phase 4.6.

**Reviewed 2026-06-24 against the `phase-3` branch (now ~30 commits ahead of `master`).** Phase 3 is largely implemented but the branch has moved well past the original plan, with a *second* library-wide rename and new infrastructure. The active working list is `scratch/todo.md` — this section is reconciled against it. Architecture facts this section now assumes:
- **`provider` → `connection` rename.** The fetcher interface is now **`Email_Connection_Interface`** (`includes/api/interface-email-connection.php`), slimmed to just `get_friendly_name()` + `test_connection()`. Pulling email moved to a separate **`Supports_Fetching`** interface (`includes/api/interface-supports-fetching.php` — `retrieve_emails()` + read/delete capability methods); credentials to **`Requires_Credentials`**. Receive-only connections (AWS SES→SNS→REST, Cloudflare routing) implement `Email_Connection_Interface` but **not** `Supports_Fetching`; UIs gate the "Check now" button on `instanceof Supports_Fetching`. The IMAP/Gmail classes moved under `includes/connections/`. ⚠️ Note the resolution filter is **still named `bh_wp_mailboxes_provider_for_account`** (not updated to "connection") — an inconsistency to reconcile; and `scratch/todo.md` still lists "Rename connections → connectors", so these paths/names may shift again.
- **Per-email action renamed.** The old `bh_wp_mailboxes_fetch_emails_saved_{slug}` / `_complete` hooks are replaced by **`bh_wp_mailboxes_new_email`** fired once per saved email with `( string $plugin_slug, BH_Email_Account $account, New_Email_Interface $new_email )` (`class-api.php:182`). `New_Email_Interface` has `New_Email_Local` / `New_Email_Remote` variants built by `New_Email_Factory`; result models are now `Check_Mailbox_Result` / `Check_Email_Account_Result` returning `New_Email_Interface[]`.
- **Mailbox registry filter `bh_wp_mailboxes_registered_mailboxes`** (`( array $mailboxes, string $plugin_slug )`) lets a plugin enumerate its mailbox API instances; consumed by `includes/wp-includes/class-cli.php` and the dev plugin's `admin/class-menu.php` + `admin/class-settings.php`.
- Credentials filter `bh_wp_mailboxes_credentials` is `( $credentials, string $plugin_slug, BH_Email_Account $account )`. ⚠️ **Confirmed bug (not just latent):** `API::test_connection()` (`class-api.php:338`) calls it with **arguments in the wrong order** — `apply_filters( 'bh_wp_mailboxes_credentials', $plugin_slug, null, $account )` instead of `( null, $plugin_slug, $account )` as at lines 233 and 472. A correctly-typed 3-arg callback receives a string where it expects `?Account_Credentials_Interface` → `TypeError`. Fix and add a `test_connection` unit test that registers a real 3-arg credentials callback. **Resolved (verified 2026-06-25):** all four `apply_filters( 'bh_wp_mailboxes_credentials', … )` call sites now use `( null, $plugin_slug, $account )`; regression test `test_test_connection_resolves_credentials_via_filter_with_correct_argument_order` is present in `tests/unit/api/class-api-unit-Test.php`.
- Failed-login backoff is gated on `wp_doing_cron()` (manual fetches always attempt). `API` also gained `update_email_local_status()`, a post-download mark-read/delete loop, and remote-action methods routed through the connection + `Remote_Email_Coordinates` with severity-tagged log notes.

Status of each task:

- [x] 3.1 **Done, with deviations.** `development-plugin/connections/class-mock-mailbox-fixtures-connection.php` (`Mock_Mailbox_Fixtures_Connection`) implements `Email_Connection_Interface` + `Supports_Fetching`, parses the 5 bundled `.eml` files (`development-plugin/connections/fixtures/*.eml`) via `MailMimeParser`, and self-registers on `bh_wp_mailboxes_provider_for_account` from its constructor. The `fixture@example.com` account is added in `development-plugin.php` (guarded). It does **not** implement `Requires_Credentials`, so no dummy credentials filter is needed. Per-user read/unread/deleted state is tracked in user meta with a `reset()`. **Follow-ups:** (a) ⚠️ **no test coverage** — add `tests/unit/.../class-mock-mailbox-fixtures-connection-unit-Test.php` (fixture count, coordinate/read-state mapping, `reset()`); (b) `retrieve_emails()` **ignores `$since_time`** (returns all fixtures) — decide if acceptable for a demo, fix the docblock either way; (c) remove any dead `set_credentials()` left over from the provider era (the class shouldn't carry it unless it implements `Requires_Credentials`); (d) correct the stale class docblock ("JSON files", "always reported as read"). **Follow-ups (a)–(d) completed 2026-06-25:** added `tests/unit/development-plugin/connections/class-mock-mailbox-fixtures-connection-unit-Test.php` (friendly name, 5-fixture mapping, `reset()`); `retrieve_emails()` docblock already states `$since_time` is ignored (acceptable for a demo); removed the dead `set_credentials()`/`$credentials`/`Account_Credentials_Interface` import; corrected the file/class/property/`get_is_marked_read` docblocks. (`development-plugin/` added to the `autoload-dev` classmap so its classes are unit-testable.)
- [x] 3.2 **Largely done 2026-06-25.** Extracted a named, parameterized `Mailbox_Settings` (`development-plugin/mailboxes/class-mailbox-settings.php`) replacing the three inline anonymous `BH_WP_Mailboxes_Settings_Interface` classes in `development-plugin.php`, and a named `Fixtures_Account_Settings` (`mailboxes/class-fixtures-account-settings.php`) replacing the inline `$fixtures_settings`. Unit tests added for both; `composer dump-autoload` run. **Remaining nit:** the `Email_Account_Settings_Interface` impls returned by `mailboxes/class-imap.php` / `class-gmail-api.php` are still anonymous (but already live in named helper files). Original note: The dev plugin now has named admin classes `admin/class-settings.php` (`Settings`, with a "Run now" button → `$api->check_email()`, ENV-vs-transient IMAP credential fields, cron schedule display) and `admin/class-menu.php` (`Menu`, driven by the `bh_wp_mailboxes_registered_mailboxes` registry). This satisfies the `scratch/todo.md` "[x] Add development plugin settings page" items. **Still outstanding:** the `BH_WP_Mailboxes_Settings_Interface` implementations remain **anonymous classes** — now *three* inline in `development-plugin.php` (`$imap_mailboxes_settings` ~L149, `$gmail_mailboxes_settings` ~L216, `$fixtures_mailboxes_settings` ~L256), and the `Email_Account_Settings_Interface` impls are anonymous too (`mailboxes/class-imap.php`, `class-gmail-api.php`, and the inline `$fixtures_settings`). Extract these into named, PhpDoc'd reference classes (or one parameterized class) so the README (Phase 5) can link a real file; align with the existing `tests/_support/models/BH_WP_Mailboxes_Settings_Fixture.php`. **Tests:** unit test of the named settings getters. Run `composer dump-autoload`.
- [x] 3.3 **Core done 2026-06-25 — option (a) REST chosen.** `development-plugin/rest/class-mailboxes.php` now registers `POST /fetch` (enumerates `bh_wp_mailboxes_registered_mailboxes`, calls `check_email()->get_emails()` per `API_Interface`, returns `{ fetched }` — mirrors the Settings "Run now" path, tolerant of unreachable mailboxes) and `DELETE /emails` (force-deletes every `fixtures_email` post and clears the three `_mock_mailbox_fixtures_provider_*` per-user meta keys for all users via `delete_metadata('user', 0, …, true)`, returns `{ deleted }`). The **Reset button is now wired**: `Mock_Mailbox_Fixtures_Connection::handle_reset_submission()` (hooked `load-edit.php`) verifies the `bh-wp-mailboxes-reset-fixtures` nonce, calls `reset()`, and redirects back to the list table. `composer lint` clean; full unit suite green (Playwright coverage of fetch/reset lands in Phase 4.2). **Still outstanding (deferred to Phase 4 e2e infra):** `scratch/todo.md` "[ ] E2E tests should use their own mailbox … not included in the menus" (arrange mailbox separate from the visible fixtures mailbox) and "[ ] Get rid of the development-plugin authentication class; use WP E2E test utils or Application Passwords".
- [x] 3.4 **Done 2026-06-25.** Added `development-plugin/class-example-integration.php` (`Example_Integration`), hooked `bh_wp_mailboxes_new_email` (3-arg `string $plugin_slug, BH_Email_Account $account, New_Email_Interface $new_email`), logging each new email's subject via the configured `bh-wp-logger` and recording a note on the email's own log via `New_Email_Interface::add_local_note()`. Wired in `development-plugin.php` (registered right after the logger is created). Unit test added. This is the snippet the README (Phase 5) points to and advances the `scratch/todo.md` "log the new-email action" item.

**Done when:** `wp-env start` → fetch (REST `/fetch` *or* Settings "Run now") with the fixtures connection → fixture emails appear in the WP_List_Table without real credentials; `Mock_Mailbox_Fixtures_Connection` and the named settings class each have a unit test; the Reset button works; the `bh_wp_mailboxes_credentials` arg-order bug in `test_connection()` is fixed and regression-tested; an example `bh_wp_mailboxes_new_email` consumer logs subjects; `composer lint` is clean. (The new Reset UI control triggers rule 3 → a Playwright spec belongs in Phase 4.)

## Phase 4 — End-to-end Playwright suite

**Rewritten 2026-07-01.** The suite has grown to seven specs (~1,150 lines): `plugin-status`, `single-email-view` (526 lines — metaboxes, iframes, immutable title, remote-status radio + Update persistence), `emails-list-controls` (check button, "Sent" column, "Trash locally"/"Delete on server" row actions), `status-view`, `status-view-interactions` (Check now / Since flows, "no new emails" on second check, new-row highlight), `dev-plugin-menu`, `dev-plugin-settings`. Original 4.3/4.4 are substantially delivered; the tasks below are the *remaining* gaps. Conventions to keep: arrange via dev REST `bh-wp-mailboxes-dev/v1` (`/status`, `/accounts`, `POST|DELETE /emails`, `POST /fetch`), UI only for the behavior under test; test naming per rule 4.

- [x] 4.1 **Done.** `tests/e2e-pw/specs/emails-list-table.spec.ts` (7 specs): after a per-account `POST /fetch`, all 5 fixture emails render; the search box finds a fixture by subject (`s=weekly`, unique to the DMARC fixture); the `restrict_manage_posts` account dropdown filters by account. Implemented the dropdown (`Emails_List_Page::table_filters()` was a stub) + a `filter_by_account()` `pre_get_posts` handler filtering on `post_parent` (emails store their account as `post_parent`); the dropdown renders only when ≥2 accounts exist. Bug (a): guarded `show_all_post_statuses()` to respect an explicit `?post_status=` selection instead of always forcing `any`. Bug (b): `row_actions()` now unsets the `inline hide-if-no-js` Quick Edit action. wpunit tests added for all three (`class-emails-list-page-wpunit-Test.php`). To arrange deterministically under `fullyParallel`, the dev REST `POST /fetch` gained an optional `account_id` (fetch one account) and `POST /emails` an optional `account_id` (→ `post_parent`). **Discovered (not fixed, out of scope):** the fetch pipeline calls `wp_tempnam()` (from `wp-admin/includes/file.php`) in `Email_WP_Post_Repository::save_attachments()` *outside* the per-attachment try/catch — it fatals when attachments are enabled and the fetch runs outside admin (e.g. real wp-cron, or plain REST). The dev `/fetch` endpoint now `require_once`s that file to work around it; the library itself should guard/load it.
- [x] 4.2 **Done.** `tests/e2e-pw/specs/fetch-pipeline.spec.ts`: a per-account `POST /fetch` returns `{ fetched: 5 }` and five rows appear; a second returns `{ fetched: 0 }` and the rows are identical (asserts on the row post-ids, not just the count). Reset-button spec (owed from 3.3): clicking Reset runs the nonce-verified handler (asserted via its redirect to the unfiltered list) and a re-fetch still yields exactly five rows with no duplicates. **Bug fixed:** the fixtures Reset button's nonce field was named `_wpnonce_checknow` — identical to the `Emails_List_Page` "Check now" nonce field in the same form — so the two collided and `handle_reset_submission()`'s nonce check failed (Reset silently did nothing). Renamed the reset field to `_wpnonce_reset_fixtures`. (Noted for 4.6.1: the singleton fixtures connection resolves remote coordinates against the original `fixture@example.com` account, so delete/mark actions on other accounts' emails address the wrong post; and per-user fixture state is shared across the single admin user — both are the shared-state problems 4.6.1 addresses.)
- [x] 4.3 **Done, with deviation.** Added an XSS test to `single-email-view.spec.ts`: an HTML body carrying an inline `<script>` (which also `alert()`s) and an `<img onerror>`, each trying to set a flag on the parent window. Asserts the iframe `sandbox` attr is exactly `allow-same-origin` (no `allow-scripts`), that the dangerous markup **is** present in the `srcdoc` (so it's the sandbox neutralising it, not sanitisation stripping it), and that no flag was set and no dialog fired. The console confirms *"Blocked script execution … sandboxed and 'allow-scripts' permission not set."* **Deviation from the literal plan:** the payload is arranged via `POST /emails` with `body_html` (how this spec already arranges its HTML-body tests — it does not use the bundled `.eml` fixtures), exercising the **identical** `Single_Email_View::render_html_content_metabox()` iframe path. A 6th bundled `.eml` was *not* added: it would only ripple the fetch-count assertions (5→6) across `emails-list-table`/`fetch-pipeline`/`status-view` with no benefit to this render-path test, and the fixture-count unit test therefore stays at 5. (Count-changing fixtures belong in 4.6.1's dedicated arrange mailbox.) The rendering code already sandboxed correctly, so no feature change was needed.
- [ ] 4.4 `status-view.spec.ts` extension: after `POST /fetch`, the account card shows a real last-fetched time (not "Never") and an email count of 5. (The "Just now" AJAX path is already covered in `status-view-interactions`; this pins the server-rendered values.)
- [ ] 4.5 `auth-failure-notice.spec.ts` — ⚠️ **feature first**: `includes/admin/class-admin-notices.php` is a 20-line stub, so there is no notice to test ("Admin notice when failed to connect" is still open in `scratch/todo.md`). (a) Implement minimally: on fetch failure (where `last_failed_login_time` is recorded) enqueue a dismissible per-account admin notice; clear it on next success. Unit tests per the Phase 2 table (Mockery). (b) Add a dev-REST-settable flag that makes `Mock_Mailbox_Fixtures_Connection::retrieve_emails()` throw. (c) Spec: set flag → fetch → notice appears naming the account; clear flag → fetch → notice gone. Scope note: "no immediate retry" backoff is gated on `wp_doing_cron()`, so don't assert it through the manual-fetch UI — assert instead (via `/status` or the status card) that `last_failed_login_time` was recorded.
- [ ] 4.6 e2e infrastructure (deferred from 3.3; items from `scratch/todo.md`):
  - 4.6.1 Dedicated arrange mailbox: register a separate e2e-only mailbox in the development plugin, excluded from admin menus, so specs that create/delete emails stop sharing state with the human-facing fixtures mailbox.
  - 4.6.2 Replace `development-plugin/class-authentication.php` (`?login_as_user=` shortcut used by `tests/e2e-pw/setup/auth.setup.ts`, plus its treat-REST-callers-as-admin behavior) with `@wordpress/e2e-test-utils-playwright` built-in auth and Application Passwords for REST; delete the class.
  - 4.6.3 Global teardown: fail if `wp-content/debug.log` is non-empty, with an allowlist (e.g. "Automatic updates starting…"). Playwright config currently has no `globalTeardown`.
- [ ] 4.7 Suggested order: 4.6.1 → 4.1/4.2/4.4 (share the fetch/reset arrange helpers — extract into `tests/e2e-pw/helpers/`) → 4.3 → 4.5 → 4.6.2/4.6.3 last (touch every spec).

**Done when:** all specs pass locally and in `e2e.yml`; `class-authentication.php` is deleted; `Admin_Notices` is no longer a stub and has unit + e2e coverage; the post-status filter bug is fixed with a regression spec.

## Phase 5 — Documentation

Existing E2E tests should have steps added to take screenshots relevant to documentation (in a way that regenerates in newer releases).

- [ ] 5.1 Rewrite `README.md`: what it does (keep the existing examples) → installation (`composer require brianhenryie/bh-wp-mailboxes`) → 5-minute integration walkthrough referencing the development-plugin files from 3.2/3.4 → actions & filters reference → Gmail API setup (keep existing section) → GDPR section (keep, tighten). Move the CLI sketch, credentials-storage questions, and "Should we keep a historic count" to `docs/open-questions.md`.
- [ ] 5.2 Generate the hooks reference with `pronamic/wp-documentor` into the README's existing `<!-- filters -->` markers; add a CI step in `phpcbf.yml` that fails if the committed output is stale.
- [ ] 5.3 Update `ARCHITECTURE.md` with a diagram: Cron → `API::check_email()` → Fetcher (IMAP / Gmail / filter-supplied) → `Email_WP_Post_Repository` → CPT → action. Document the settings/credentials split (library never stores credentials).
- [ ] 5.4 Populate `CHANGELOG.md` (currently 13 bytes) following keepachangelog.com; document dev setup in `CONTRIBUTING.md` (wp-env, suites, e2e).
- [ ] 5.5 Fix the README badge row: "WordPress tested 7.0" links to a wordpress.org plugin page that does not exist for a library — point badges at the GitHub repo/actions instead.

**Done when:** a developer can integrate the library using only README + development-plugin, and every documented hook exists in code.

---

## Verification checklist (run at the end of every phase)

```bash
composer lint                       # phpcbf + phpcs + phpstan
vendor/bin/codecept run unit
vendor/bin/codecept run wpunit
npm run wp-env:start && npm run test:e2e
```

## Out of scope (do not do without explicit user approval)

WP-CLI commands; credentials persistence; autodelete/retention scheduling changes; Gmail OAuth UI flows; publishing to wordpress.org; git history rewriting (flag it, don't do it).
