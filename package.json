{
  "name": "bh-wp-mailboxes",
  "private": true,
  "description": "Development tooling (wp-env + Playwright) for bh-wp-mailboxes.",
  "scripts": {
    "wp-env": "wp-env",
    "wp-env:start": "wp-env start --xdebug",
    "wp-env:stop": "wp-env stop",
    "test:e2e": "tsx node_modules/.bin/playwright test",
    "test:e2e:ui": "tsx node_modules/.bin/playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.60",
    "@wordpress/e2e-test-utils-playwright": "^1.50",
    "@wordpress/env": "^11.10",
    "dotenv": "^17.4",
    "tsx": "^4.22"
  }
}
